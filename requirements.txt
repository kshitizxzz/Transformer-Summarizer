torch>=2.1
numpy>=1.24
pandas>=2.0
streamlit>=1.30
plotly>=5.18
matplotlib>=3.7
tqdm>=4.66
jupyter>=1.0
ipykernel>=6.25
datasets>=2.19,<3.0
rich>=10.14,<14
# Optional: for NER (falls back to rule-based if not installed)
spacy>=3.5
# Optional: download spaCy model separately:
#   python -m spacy download en_core_web_sm
